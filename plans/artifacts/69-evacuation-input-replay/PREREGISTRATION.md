# Evacuation continuity: selected input-conflict diagnostic

2026-09-05, before calls.

1. OBSERVED in one session, bb72dc94. A text-only reader of T5–12 identified students already in quartets and departing at T9, then back at their marks awaiting group formation at T10. The T9 Director itself supplies the completed change in `time_skip_summary`: “Os alunos formam grupos hesitantes e começam a se mover em direção à rota de serviço, enquanto Garran mantém a ordem e o alarme continua soando.” The T10 Director request's transcript says “Os dezesseis selecionados, agora divididos em grupos de quatro, movem-se em direção à abertura nos fundos”. Yet its ROTEIRO says “Not in play yet — introduce as concrete perception events: pergaminho rasgado na mão de Garran, portões internos se fechando com estrondo, ferimento no ombro do batedor” and “The beat ends when: Garran Holt finalmente obtém a autorização de Maelis para conduzir a evacuação pela rota de serviço, e os alunos começam a se mover em grupos hesitantes; o sino de alerta continua a tocar ao fundo.” Its archived response sets `group_formation` to “hesitante, quartetos ainda não formados”. These are conflicting supplied cues. Whether the two ROTEIRO lines contribute to the reset is THEORY.

2. Select the last successful recorded Director request at T10 in `plans/artifacts/repetition-battery/base-P1-r2/sessions/bb72dc94/debug.jsonl` (line 107 at preparation). Freeze source SHA, line, original response and exact messages in the manifest. This case was selected after a literary read, not randomly; the preceding roof diagnostic failed its follow-up gate and is not replaced by this case. This is an additional case for the existing continuity investigation, not a corpus estimate.

3. Four actual curl calls per arm, eight total, randomized seed 691005, concurrency four. A retains the exact archived request. B removes only the two ROTEIRO lines headed `Not in play yet — introduce as concrete perception events:` and `The beat ends when:`. Keep Current act, Current beat, facts, history, all other instructions, model, max_tokens, JSON format and disabled thinking unchanged. Assert exactly one removed occurrence per header. This tests bundled deletion and shorter input, not semantic conflict separately from planning removal. Preserve requests, envelopes, session/turn/agent, HTTP status, timing, script/preregistration hashes. Credential through curl config stdin only. No live-session writes. Historical prompt markers remain unchanged in the private replay.

4. Validate all envelopes against the archived response schema. Missing, invalid or rate-limited outputs stay in the record and are not replaced. A fresh reader receives the preceding Link-visible T5–9 narrative, speech and actions, then all returned Director `perception_events`, `time_skip_summary`, `scene_update`, `scene_blocking` and `zone_moves` projected as fictional propositions with canonical names. Remove routing, moods, technical field labels, prompts and variant labels. Preserve whether a proposition describes a current position or an event; these are Director continuations, not rendered prose or full downstream conversations. The reader is asked only whether each continuation follows the scene, with quotations, strengths, ambiguities and contradictions. The reader is not given our reset hypothesis or classification rubric. Opaque order seed 691006; the primary investigator reads and classifies the opaque full-output dossier after the free reader returns, saves those judgments, and only then opens the separate arm key. Every source field is checked separately before that join. The investigator knows the design, and generated content can reveal clues; concealing labels is not complete blinding.

5. After the free literary reading, the primary investigator classifies each full Director output, initially with arm labels concealed, using these predeclared categories: RESET = asserts quartet formation has not yet occurred or returns the already departing students to the original waiting stage without a new reversing event; CONTINUATION = treats formed/departing groups as established while continuing or interrupting their evacuation; ABSENT = output gives no basis to assess formation/departure continuity; AMBIGUOUS = field disagreement, unclear subgroup, or plausible continuation versus reset that cannot be resolved. A new obstacle stopping already formed groups is not RESET. Repeating an order is not RESET by itself. Record quotations, disagreements with the free reader and unrelated harms; no lexical similarity or movement count. Schema-invalid projections can be discussed qualitatively but cannot satisfy the follow-up criterion.

6. Fixed decision rule for this one payload: only if all eight outputs are schema-valid, A has at least three RESET, B at most one RESET, and neither arm has ABSENT or AMBIGUOUS, pursue a separate prospective study of the implicated planning boundary. Otherwise this test does not pass the follow-up criterion. This gate measures within-payload repeatability only; it does not approve a production change or establish a general quality gain. Reader concerns can veto a proposed benefit even when the count clears this diagnostic gate. Publish every category and concrete concern, not a winner percentage. The model name does not guarantee unchanged provider behavior since the archive.
