# Main-agent source adjudication, before unblinding

The independent source-agent dispatch and a follow-up dispatch were refused
by the tool's thread limit. This is the main agent's source check, not the
separate isolated source reader promised by the preregistration. The literary
reader remains isolated. The deviation prevents claiming the full planned
reader procedure was completed.

- V1: NO TRANSITION for the original messenger. No event releases or moves him;
  the retained east-door fact remains compatible. Other-state concern: the
  selection is converted, but `fileira_central` still says the next reading
  should proceed. Do not infer that the messenger had to move this turn.
- V3: SUPPORTED for the messenger's fall/movement at the threshold. The full
  response contains “O mensageiro ferido, arrastado pela porta leste por Elowen,
  cai na soleira”. This event excludes C1 from witnesses and therefore is absent
  from the literary projection, but is present in the source. The old retention
  clause is rewritten and `porta_leste` describes the fallen messenger. Other
  concerns remain: the crystal changes from explicitly extinguished to “ainda
  vermelho” without an event; the fragment is still in his boot in the event
  but “recolhido” in state without a collection event. This focal classification
  is not whole-output acceptance.
- V4: NO TRANSITION for the original messenger. The event explicitly introduces
  “um segundo funcionário”; retaining the original messenger does not put the
  same person in two places. `selection_status` describes groups as formed
  after an order to form them, without a formation event.
- V7: AMBIGUOUS for the messenger. Falling inside and lying at the threshold
  does not necessarily end Elowen's detention; the old “retido” statement alone
  does not prove a second incompatible location. The selection-state problem
  has a program contribution: raw `selecao_status` says reading is suspended,
  but the current key guard discards that key, leaving `selection_status`
  saying reading resumed. The raw update also keeps other old facts unchanged.
- V8: NO TRANSITION for the messenger. He remains retained without a movement
  event. Separately, candidates being ordered away from marks is not an event
  confirming that all have moved, as the new `fileira_central` asserts.
- V2, V5, V6: ineligible; no enacted-continuation judgment.

These are contextual readings, not a calibrated detector or an independent
replication. No arm identity was consulted for this adjudication. The local
retention-code fix made after preparing the projections affects eviction order
at capacity, not the model calls or the messenger interpretation here.

## Correction after the key was read

The V8 classification above is wrong. A later explicit check of events excluded
from C1's witnesses found: “Irmã Elowen Ramires solta o mensageiro e abre caminho
até a plataforma, com o envelope selado ainda intacto contra o peito.” The raw
response resolves release while the projected state still says the messenger
is retained. V8 is CONFLICT, not NO TRANSITION. The original judgment remains
above to preserve the error and the timing of its correction. The main agent
had not checked all non-viewer events when writing the initial adjudication;
it must not be represented as a complete blind source audit.
