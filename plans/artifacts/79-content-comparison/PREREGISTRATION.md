# Prospective content comparison for current-turn blocking

2026-09-05, before provider calls. This follows a text-first reading of two archived post-79 scenes, not a detector sample. Selected renderer inputs are bb72dc94 T9 (evacuation instruction) and ea6620fb T12 (beam collapse and crossing). The study is targeted and cannot estimate corpus quality.

For each actual final recorded prose request, send three replicas unchanged and three with only the entire BLOCKING section removed. Retain all other messages, visible transcript, events, roster and retry correction if present. Preserve original model, output format and max_tokens. Use actual curl, credentials only through stdin, exact requests/responses and source hashes saved; at most six simultaneous calls among twelve total, call order randomized with seed 790906. Do not replace invalid calls. Outputs never enter a live session.

Two fresh readers each receive one scene's preceding visible narration/dialogue plus three opaque pairs of candidate next narrations. Pair same-index replicates, then randomize pair order and displayed side using a separate seed 790907. Neither gets code, events, expected winner, method name or numerical scores. Ask which continuation, if either, lets them follow the scene more naturally; require quoted evidence, acknowledge missing context, and distinguish stylistic taste from continuity. They may reject both, prefer neither or find insufficient evidence. No numeric quality grade or inference from verbosity/movement frequency.

After their reading, the primary agent checks each purported improvement against the exact confirmed events supplied to the renderer. A vivid invented accomplishment cannot count as evidence for retaining a variant. Paired prose is assessed before downstream character replies: attaching the archived replies to a counterfactual narration would manufacture an unmatched continuation. Readers can assess the immediate sequence, not the effects on an entire future session.

Decision fixed in advance: retain the current production path and keep quality acceptance open regardless of this small diagnostic. Record text-grounded preferences and source violations case by case, with no pooled win percentage. There is no automatic follow-up gate: any further experiment requires its own concrete hypothesis and prospective rule, supported by the passages actually found here. Divergent preferences across these different scenes do not measure reader reliability. This diagnostic does not decide whether to revert the shipped path and cannot authorize persistence. The sample has two sessions; repetitions are stochastic observations within their original scene, not new sessions. Both favorable and unfavorable passages remain in the report.

Selection rationale: the first reader identified uncertainty about Link after the beam falls behind the group; the second identified formed quartets returning to their marks. These are targeted probes of whether staging helps an immediately understandable continuation, not a representative sample. Source checking covers every output, including disfavored ones; distinguish incidental sensory description from a new action, arrival or completed outcome absent from confirmed events.
