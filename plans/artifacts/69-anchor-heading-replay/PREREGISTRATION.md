# Scope and experiment: the anchor list must not assert absence

1. OBSERVED in one archived session, bb72dc94, T9→T10. The beat references “portões internos se fechando com estrondo”. The Director's confirmed T9 event is “Os portões internos da masmorra se fecham com um estrondo metálico.” Executing the production matcher on that exact pair returns false. The archived T10 record retains only the dust anchor as seen, and the actual Director request says “Not in play yet — introduce as concrete perception events: ... portões internos se fechando com estrondo ...”. This is an unsupported absence claim produced by the prompt builder. It is not yet a demonstrated cause of narrative repetition.

2. Proposed bounded correction: replace that heading with `Elements referenced by this beat:`. Keep the anchor strings, their current filtering, all other instructions, the exit condition, state and history unchanged. The heading will describe the supplied list without asserting that those elements have never appeared or ordering their introduction. This makes the heading truthful on the demonstrated mismatch; it does not repair lexical coverage, prove beat completion, or claim improved literary quality. This correction changes no coverage algorithm, replanning trigger or persisted field. It also removes the introduction command in the old heading; the intervention tests both textual changes together.

3. Pre-register two archived Director payloads: bb72dc94 T10 (evacuation) and 8bd4d0f1 T34 (roof). Both have already been examined; their previous deletion experiments remain inconclusive and are not replaced. Freeze the last successful recorded request per turn and source hashes. Run four curl calls per original/neutral-heading arm, sixteen planned model responses, randomized seed 691105, concurrency four. Change exactly one heading occurrence per modified request. Preserve every other message byte and provider parameter. Allow at most one separately logged retry per planned call only for a curl connection failure with no HTTP response; never replace a returned invalid or unfavorable model output. Validate against each archived schema and preserve envelopes, timing, source, session/turn, agent and script hash. Read all returned outputs, including invalid projections, with missing/invalid calls explicit.

4. Decision before results: this screens for regression while correcting a demonstrated unsupported prompt assertion. Require at least three schema-valid outputs per arm per payload, and no more schema-invalid returned outputs in B than in A for either payload. A fresh reader per scene receives preceding fiction and shuffled independent continuations with canonical names, no prompts, code, arm labels or expected conclusion. Save its unconstrained judgments before joining arms. The primary investigator maps every concrete concern to its source, preserving strengths and uncertainty. Block if a supported harm occurs in at least two more valid B outputs than valid A outputs in either scene; this covers both new harms and aggravation of existing harms. Also block any unresolved material deterioration supported by contextual reading, even below that recurrence threshold, and any clear agency or confidentiality violation in a modified continuation. This is a local, uncalibrated screen with an explicit qualitative veto; it establishes neither equivalence nor literary improvement. If incomplete or blocked, do not ship based on this test. If it passes, integrate the exact tested heading, add a deterministic check for the demonstrated misleading prompt, and run roteiro tests, per-viewer and prompt-invariant tests, ruff check/format, mypy and pytest. No winner rate, population claim or causal attribution follows.
