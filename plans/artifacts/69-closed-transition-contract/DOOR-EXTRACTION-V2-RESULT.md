# Hall-gate proposal extraction: second local screen

The registered V2 screen passed its local rule: all 12 curl calls returned HTTP 200 and schema-valid JSON, with T7 `no_change` in 4/4, T8 `change` in 4/4, and T10 `no_change` in 4/4. The calls used three accepted Director proposals from one archived session, each sampled four times; this is one session, not twelve independent narrative cases. The `door-extraction-manifest.json` freezes the source, script, preregistration and requests; `door-extraction-runs/` holds each request, provider envelope, distinct response ID, parsed result and grade.

I reread the source proposals beside the outputs. T7's main gates start semiopen; a chest opens, but no event or update opens the gates. T8's gates start semiopen, then `se escancaram`, and the update says `abertos por impacto externo`. T10's entrance starts open; its doors burst inward and remain open in both the later event and update. The extractor's aperture labels match those three textual readings, but `no_change` on T10 alone cannot prove it noticed the impact; T8 is the only positive opening control. T10 repeats an earlier wounded-guard entrance and may have a continuity defect about the guard or gate impact. That question was not scored as door aperture and this result does not clear it.

The screen tests one named existing door with manual current-state fixtures and explicit update wording on the positive turn. It does not test creating an entity, identifying an ambiguous gap, runtime producer integration, or whether a renderer preserves a proposed physical fact. The next diagnostic is a proposal-to-renderer fidelity test on a real accepted Director proposal; this result alone does not satisfy Task 69's producer or live-quality requirements.
