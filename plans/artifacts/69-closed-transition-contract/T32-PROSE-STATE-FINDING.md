# T32 physical state/prose divergence, source-checked 2026-09-24

In archived session `drive-P1-r1/5c994c42`, turn 32, the accepted Director event says Bruna's discharge partially disperses a dust creature. Its same-turn `scene_update.dust_creature` says `dispersando em espirais, mas ainda ativa perto da borda da fenda`. The renderer's first response says the creature begins to regroup and remains poised to advance. A second renderer response, requested after a lexical-repetition correction, instead says the particles disappear `sem deixar vestígio`, the creature's remains dissipate, and only a fading sulfur smell remains where it stood. The latter response is the narration persisted in `state.json` for turn 32. The next Director request, T33 of the same debug log, still carries `dust_creature: dispersando em espirais, mas ainda ativa perto da borda da fenda` in its physical facts. The divergence therefore reached both reader-facing history and the next turn's state context. One isolated model reader, given the event, update and both prose variants but no code, found the first compatible and the persisted second irreconcilable with the creature still being active. The second response's physical absence is an asserted outcome, not a mere omission.

The two prose calls share the same base request; the second adds only a user correction asking for fresh phrasing after a near-verbatim echo. This is an observed contradiction after a retry request; whether the lexical correction contributed is undiagnosed. The case is one turn in one archived session, with no prevalence estimate. It is a source-checked example for Task 81's state/prose mismatch; the previously withdrawn actor and gap examples remain withdrawn.

For Task 69, the case motivates testing fidelity from the accepted proposal to the rendered prose before trusting extracted transitions. A narrow next experiment can replay the exact accepted T32 retry request by curl, compare the existing correction to a correction that explicitly preserves the confirmed physical outcome, and read every output against the accepted event and scene_update. Pre-register the decision rule first; a screen pass alone would not justify production.
