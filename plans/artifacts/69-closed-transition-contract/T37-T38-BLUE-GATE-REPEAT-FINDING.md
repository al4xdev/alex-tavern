# Blue gate closes twice across a time skip, source read 2026-09-24

In archived `p1-archive/null-P1-r1/7fd84e9a`, the accepted T37 Director output asks for three time-skip ticks. Its `time_skip_summary` says the blue team disappears into the tunnel and `o portão se fecha com um baque surdo`; its same-turn `scene_update.gate_blue` is `fechado` and `selection_status` says all gates are sealed (debug line 344). The Runner records the skip (line 345). The actual T37 renderer request includes that summary as a confirmed observation (line 348), and the persisted narration says `O portão azul range um último protesto, as folhas de metal se encontrando com um baque surdo` and then `os quatro portões permanecem lacrados`. The closure reaches both state and reader-facing history before T38 starts.

At T38, the accepted Director again has the blue team cross a narrowing gap `antes que ele se feche`, then says `O portão azul se fecha com um baque surdo` (line 350). Its `scene_update.gate_blue` remains `fechado`. The T38 renderer request receives that second closure as a confirmed `physical_outcome` (line 351), and persisted narration says `As folhas do portão colidem com um baque surdo`. Neither accepted turn contains a gate reopening between the two closures. An independent text-only reader of both complete event channels, state updates, renderer event blocks and persisted prose judged the T38 closure a repeat of the completed T37 closure, not a distinct later closure.

This is one source-grounded physical restaging case for Task 69, with no prevalence estimate. T38 also restages the crossing before closure: T37's summary already says the team disappears into the tunnel, while T38 sends Téo and others through a closing gap. The character positions and the gate aperture need separate assessment; this finding labels only the repeated closure. A storage layer that rejects a `closed -> closed` transition **without rejecting or revising the proposed prose event** would leave the duplicate visible, but no particular repair path is assumed. No candidate repair, extractor accuracy or model mechanism was tested here. The [source packet](T37-T38-BLUE-GATE-SOURCE-PACKET.txt) contains the complete quoted channels. A later diagnostic should test a real proposal with the already-closed gate state and inspect both persisted state and rendered prose.
