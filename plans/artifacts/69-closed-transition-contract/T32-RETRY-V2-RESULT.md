# T32 renderer retry: local screen incomplete

The [registered one-payload rule](T32-RETRY-V2-PREREGISTRATION.md) did not reach its technical gate. Eight curl calls returned HTTP 200 and distinct response IDs, but only seven passed local `{"narration": string}` validation. A4 included an unexpected `additionalProperties` key and was rejected. The rule required **8/8 valid** and prohibited replacement calls, so no semantic A/B gate was scored. The [manifest](t32-retry-manifest.json) holds source, script and preregistration hashes plus both exact requests; [run artifacts](t32-retry-runs/) hold each envelope, parsed result, validator outcome and [technical grade](t32-retry-runs/technical-grade.json). A direct byte scan of the manifest and all 36 run files found zero occurrences of the configured API key; this check does not cover transformed encodings.

After the technical failure, one content-only reader received a [shuffled packet](t32-retry-runs/blind-packet.txt) with the accepted Director event/update and eight prose texts, without arm labels. The [mapping](t32-retry-runs/blind-map.json) was opened after that read. Its descriptive labels were six COMPATIBLE, one CONTRADICTS and one UNRESOLVED. The CONTRADICTS text was B4: “Esses fragmentos ... somem, deixando apenas um resíduo fino.” A second independent reader found that “fragmentos” refers to expelled grains, so the text does not explicitly say the whole creature ceased to be active. The readings disagree; B4 stays **unresolved** for this report. A1 was also unresolved. A4's prose was read as compatible, but its envelope remains schema-invalid. No valid A text reproduced the archive's explicit final-disappearance claim. These readings describe outputs; they do not repair the failed gate or show a comparative effect.

The separate [source check](T32-PROSE-STATE-FINDING.md) still documents the original T32 persisted-prose contradiction. This replay does not identify its cause or support changing the retry correction. No production change follows.
