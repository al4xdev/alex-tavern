# T32 renderer retry: generic fidelity reminder, local screen V2

This is registered before new provider calls on this payload. The archived `drive-P1-r1/5c994c42` T32 accepted Director event partially disperses a dust creature; the same-turn scene_update says the creature remains active near the fissure. In the exact archived renderer retry request, the user prompt includes both the confirmed event and post-event `PHYSICAL FACTS` with the active-creature value. The archived first prose response kept the creature present; a second response, after a lexical-repetition correction, asserts that the creature's remains disappear without trace and was persisted. Whether the correction contributed to the defect is unknown. This screen compares two correction texts; it does not test retry-vs-no-retry causality or estimate a defect rate.

## Frozen arms

Use the exact archived second prose-call request from `plans/artifacts/p1-archive/drive-P1-r1/sessions/5c994c42/debug.jsonl` line 379 as arm A. Keep its system, preceding user prompt, model/settings and current final lexical-repetition correction unchanged. Arm B adds only this generic sentence to the end of that final correction: `Keep the confirmed events and current PHYSICAL FACTS authoritative; change wording without changing their physical outcomes.` No creature, dispersal, survival or other scene-specific terms enter the variant. Use the same configured DeepSeek V4 Flash endpoint, response format and disabled thinking as the archived adapter. Save source/script/preregistration hashes and both exact requests before calls. Four separate curl calls per arm, eight total, no retries or replacements. Preserve HTTP status, raw response envelope, provider ID and local JSON-schema validation. Never place the secret in CLI arguments or artifacts.

## Content reading and rule

If fewer than eight responses are HTTP-200 and schema-valid `{"narration": string}`, the screen is technically incomplete and no semantic gate is scored. Otherwise, shuffle the eight outputs with stable anonymous labels, give an isolated text reader only the accepted event, scene_update and eight prose texts, and obtain a verbatim quote and one label per text: `COMPATIBLE` when the physical account retains partial dispersal and clearly leaves the creature active/present at beat end; `CONTRADICTS` when it asserts final absence or annihilation without a subsequent recovery in the same text; `UNRESOLVED` when neither conclusion is safe. A purely figurative or viewpoint-bound disappearance is `UNRESOLVED`, not automatically `CONTRADICTS`. The reader also quotes any explicit contradiction of the other supplied Director events or scene_update fields; sensory embellishment and unmentioned background state are outside this check. Freeze the labels and rationale before unblinding. `UNRESOLVED` remains unresolved and stops the candidate; no second-reader vote relabels it. No lexical detector decides world state.

This specific candidate advances only to **a second independent source-case screen** if at least one A output is `CONTRADICTS`, all four B outputs are `COMPATIBLE`, and the reader finds no explicit contradiction of another supplied event or update in B. A zero-contradiction A result makes this comparison inconclusive; any B contradiction or unresolved output stops the candidate. Report lexical repetition separately by source quote, because this screen's question is physical fidelity; a candidate that regresses lexical quality cannot be shipped even if it meets the semantic gate. The four calls per arm are repeated samples of one prompt in one session, not independent narrative cases. Passing this screen does not authorize changing the production correction or closing Tasks 69 or 81.
