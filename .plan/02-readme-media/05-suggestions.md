# Capture 05: Suggestions popup

**README placeholder (`README.md:262`):**

```text
<place_5:screenshot of the suggestion popup with three candidate speech/action pairs>
```

## What to capture

- Open the manual suggestion interface in an active session.
- Wait for generation to finish.
- Show all three candidate options at once if the viewport allows it.
- Each visible option should demonstrate the `{speech, action}` pair rather than showing
  only a title or truncated first line.

## Deliverable

- Preferred output: `docs/media/readme/05-suggestions.webp`.
- Replace the literal `<place_5:...>` line in `README.md`.

## Done when

- Three distinct suggestions are visible and both speech and action content are legible.
