# Capture 08: Startup and preset session

**README placeholder (`README.md:465`):**

```text
<place_8:gif of ./start.sh booting and a fresh session being created from a preset>
```

## What to capture

Record the complete setup demonstration:

1. Run `./start.sh` in a terminal.
2. Show the server reaching its ready/running state on port 8889.
3. Open or switch to the application.
4. Choose an existing preset.
5. Create the new session and arrive in its fresh scene.

Do not show private shell history, tokens, private model hostnames, or unrelated filesystem
paths in the terminal capture.

## Deliverable

- Preferred output: `docs/media/readme/08-startup-preset.gif`.
- Trim dependency/setup noise that does not help demonstrate startup.
- Replace the literal `<place_8:...>` line in `README.md`.

## Done when

- The GIF visibly connects the startup command to a newly created preset-based session.
