# Capture 01: Application banner

**README placeholder (`README.md:3`):**

```text
<place_1:banner screenshot or gif of the app running a scene, chat bubbles for Narrator/Character, undo and force-speaker buttons visible>
```

## What to capture

- The application open inside an active roleplay scene.
- At least one Narrator message and one Character message visible in the chat.
- The action controls open or positioned so both **undo** and **force speaker** are visible.
- Enough surrounding UI to identify the product immediately; this is the README's first
  visual and acts as its banner.

## Deliverable

- Preferred output: `docs/media/readme/01-banner.webp`.
- A short GIF is acceptable if opening the action controls communicates the interface
  better than a still image.
- Replace the literal `<place_1:...>` line near the top of `README.md`.

## Done when

- Narrator bubble, Character bubble, undo, and force-speaker control are all legible in
  the rendered README.
