# Capture 03: Raw debug log

**README placeholder (`README.md:163`):**

```text
<place_3:screenshot of the debug/observability panel with the raw LLM call log open, showing narrator + character calls for one turn>
```

## What to capture

- Open the debug/observability drawer for a real session.
- Show two adjacent log entries from the same `turn_number`:
  - `agent: "narrator"`
  - `agent: "character:<name>"`
- Keep enough of each entry visible to demonstrate that requests and responses are logged.
- Redact or avoid private model hosts, private prompt content, or unrelated session data.

## Deliverable

- Preferred output: `docs/media/readme/03-debug-log.webp`.
- Replace the literal `<place_3:...>` line in `README.md`.

## Done when

- The shared turn number and the two different agent labels are readable.
