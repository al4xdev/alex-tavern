# Capture 04: Undo restoring mood and scene

**README placeholder (`README.md:222`):**

```text
<place_4:short gif of the undo button reverting a mood + scene change in one click>
```

## What to capture

- Begin with a completed turn that changed both:
  - at least one Character mood;
  - at least one visible scene/physical fact.
- Keep the changed values visible.
- Click undo once.
- Show both values returning to their previous state and the turn disappearing from chat.

## Deliverable

- Preferred output: `docs/media/readme/04-undo-state.gif`.
- Keep the clip focused on the before state, one undo click, and the restored after state.
- Replace the literal `<place_4:...>` line in `README.md`.

## Done when

- One click visibly restores both mood and scene state; a chat-only undo is insufficient.
